# PaymentActionType

## Example Usage

```python
from tirdad_sdk.models import PaymentActionType

# Open enum: unrecognized values are captured as UnrecognizedStr
value: PaymentActionType = "checkout_url"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"checkout_url"`
- `"payment_link"`
