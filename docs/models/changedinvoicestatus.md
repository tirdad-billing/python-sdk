# ChangedInvoiceStatus

preview | issued | INITIATED | PENDING | PROCESSING | SUCCEEDED | OVERPAID | FAILED | REFUNDED | PARTIALLY_REFUNDED

## Example Usage

```python
from tirdad_sdk.models import ChangedInvoiceStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: ChangedInvoiceStatus = "preview"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"preview"`
- `"issued"`
