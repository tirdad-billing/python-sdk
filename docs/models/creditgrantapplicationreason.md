# CreditGrantApplicationReason

## Example Usage

```python
from tirdad_sdk.models import CreditGrantApplicationReason

# Open enum: unrecognized values are captured as UnrecognizedStr
value: CreditGrantApplicationReason = "first_time_recurring_credit_grant"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"first_time_recurring_credit_grant"`
- `"recurring_credit_grant"`
- `"onetime_credit_grant"`
