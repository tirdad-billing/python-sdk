# EntitlementGrantAllocationBehavior

## Example Usage

```python
from tirdad_sdk.models import EntitlementGrantAllocationBehavior

# Open enum: unrecognized values are captured as UnrecognizedStr
value: EntitlementGrantAllocationBehavior = "first_usage"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"first_usage"`
- `"unit_start"`
