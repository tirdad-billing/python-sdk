# TaxExemptionReasonCode

## Example Usage

```python
from tirdad_sdk.models import TaxExemptionReasonCode

# Open enum: unrecognized values are captured as UnrecognizedStr
value: TaxExemptionReasonCode = "customer_exempt"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"customer_exempt"`
- `"no_tax_configured"`
