"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class UsageRecordSyncEntryTypedDict(TypedDict):
    agreement_id: NotRequired[str]
    r"""AgreementID is the subscription's identifier on this marketplace: license_arn on AWS,
    usage_reporting_id on GCP, resource_id on Azure. It records which agreement the usage was
    billed against, which stays meaningful after the connection that reported it is replaced.
    """
    connection_id: NotRequired[str]
    r"""ConnectionID is the connection that reported this entry, kept for tracing which credentials
    were used. It is deliberately not part of the map key: a replaced connection must not make an
    already reported record look unreported.
    """
    reporting_id: NotRequired[str]
    skip_reason: NotRequired[str]
    skipped: NotRequired[bool]
    synced_at: NotRequired[datetime]
    r"""SyncedAt is when the marketplace accepted the record. Zero on a skip, since nothing was sent."""


class UsageRecordSyncEntry(BaseModel):
    agreement_id: Optional[str] = None
    r"""AgreementID is the subscription's identifier on this marketplace: license_arn on AWS,
    usage_reporting_id on GCP, resource_id on Azure. It records which agreement the usage was
    billed against, which stays meaningful after the connection that reported it is replaced.
    """

    connection_id: Optional[str] = None
    r"""ConnectionID is the connection that reported this entry, kept for tracing which credentials
    were used. It is deliberately not part of the map key: a replaced connection must not make an
    already reported record look unreported.
    """

    reporting_id: Optional[str] = None

    skip_reason: Optional[str] = None

    skipped: Optional[bool] = None

    synced_at: Optional[datetime] = None
    r"""SyncedAt is when the marketplace accepted the record. Zero on a skip, since nothing was sent."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "agreement_id",
                "connection_id",
                "reporting_id",
                "skip_reason",
                "skipped",
                "synced_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
