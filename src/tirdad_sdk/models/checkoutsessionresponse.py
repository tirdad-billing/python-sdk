"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .checkoutaction import CheckoutAction
from .checkoutpaymentblock import CheckoutPaymentBlock, CheckoutPaymentBlockTypedDict
from .checkoutpaymentprovider import CheckoutPaymentProvider
from .checkoutstatus import CheckoutStatus
from .paymentaction import PaymentAction, PaymentActionTypedDict
from datetime import datetime
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class CheckoutSessionResponseTypedDict(TypedDict):
    action: NotRequired[CheckoutAction]
    cancel_url: NotRequired[str]
    cancelled_at: NotRequired[datetime]
    checkout_invoice_id: NotRequired[str]
    checkout_payment_id: NotRequired[str]
    checkout_status: NotRequired[CheckoutStatus]
    completed_at: NotRequired[datetime]
    created_at: NotRequired[datetime]
    customer_id: NotRequired[str]
    expires_at: NotRequired[datetime]
    failure_reason: NotRequired[str]
    failure_url: NotRequired[str]
    id: NotRequired[str]
    idempotency_key: NotRequired[str]
    metadata: NotRequired[Dict[str, str]]
    next_poll_after_ms: NotRequired[int]
    r"""NextPollAfterMs is how long a client should wait before reading again.
    Zero means stop — either the session is terminal, or this response did not
    come from a polling read.
    """
    payment: NotRequired[CheckoutPaymentBlockTypedDict]
    payment_action: NotRequired[PaymentActionTypedDict]
    payment_provider: NotRequired[CheckoutPaymentProvider]
    stale: NotRequired[bool]
    r"""Stale reports that this response is stored state that was not checked against
    the payment provider on this request — the read was debounced, or the gateway
    did not answer. A UI should say \"still checking\" rather than presenting a
    stale answer as fact.
    """
    success_url: NotRequired[str]
    terminal: NotRequired[bool]
    r"""Terminal reports whether the session has finished. Clients poll until this is
    true rather than hardcoding the status set, which would go stale if a status
    is ever added.
    """
    updated_at: NotRequired[datetime]


class CheckoutSessionResponse(BaseModel):
    action: Optional[CheckoutAction] = None

    cancel_url: Optional[str] = None

    cancelled_at: Optional[datetime] = None

    checkout_invoice_id: Optional[str] = None

    checkout_payment_id: Optional[str] = None

    checkout_status: Optional[CheckoutStatus] = None

    completed_at: Optional[datetime] = None

    created_at: Optional[datetime] = None

    customer_id: Optional[str] = None

    expires_at: Optional[datetime] = None

    failure_reason: Optional[str] = None

    failure_url: Optional[str] = None

    id: Optional[str] = None

    idempotency_key: Optional[str] = None

    metadata: Optional[Dict[str, str]] = None

    next_poll_after_ms: Optional[int] = None
    r"""NextPollAfterMs is how long a client should wait before reading again.
    Zero means stop — either the session is terminal, or this response did not
    come from a polling read.
    """

    payment: Optional[CheckoutPaymentBlock] = None

    payment_action: Optional[PaymentAction] = None

    payment_provider: Optional[CheckoutPaymentProvider] = None

    stale: Optional[bool] = None
    r"""Stale reports that this response is stored state that was not checked against
    the payment provider on this request — the read was debounced, or the gateway
    did not answer. A UI should say \"still checking\" rather than presenting a
    stale answer as fact.
    """

    success_url: Optional[str] = None

    terminal: Optional[bool] = None
    r"""Terminal reports whether the session has finished. Clients poll until this is
    true rather than hardcoding the status set, which would go stale if a status
    is ever added.
    """

    updated_at: Optional[datetime] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "action",
                "cancel_url",
                "cancelled_at",
                "checkout_invoice_id",
                "checkout_payment_id",
                "checkout_status",
                "completed_at",
                "created_at",
                "customer_id",
                "expires_at",
                "failure_reason",
                "failure_url",
                "id",
                "idempotency_key",
                "metadata",
                "next_poll_after_ms",
                "payment",
                "payment_action",
                "payment_provider",
                "stale",
                "success_url",
                "terminal",
                "updated_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
