"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .checkout_jsonbcheckoutconfiguration import (
    CheckoutJSONBCheckoutConfiguration,
    CheckoutJSONBCheckoutConfigurationTypedDict,
)
from .checkout_jsonbcheckoutproviderresult import (
    CheckoutJSONBCheckoutProviderResult,
    CheckoutJSONBCheckoutProviderResultTypedDict,
)
from .checkout_jsonbcheckoutresult import (
    CheckoutJSONBCheckoutResult,
    CheckoutJSONBCheckoutResultTypedDict,
)
from .checkoutaction import CheckoutAction
from .checkoutpaymentprovider import CheckoutPaymentProvider
from .checkoutstatus import CheckoutStatus
from .paymentaction import PaymentAction, PaymentActionTypedDict
from .status import Status
from datetime import datetime
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class CheckoutSessionResponseTypedDict(TypedDict):
    action: NotRequired[CheckoutAction]
    cancel_url: NotRequired[str]
    cancelled_at: NotRequired[datetime]
    checkout_invoice_id: NotRequired[str]
    r"""CheckoutInvoiceID and CheckoutPaymentID are set once the apply step
    creates the corresponding Flexprice entities (completed sessions only).
    """
    checkout_payment_id: NotRequired[str]
    checkout_status: NotRequired[CheckoutStatus]
    completed_at: NotRequired[datetime]
    configuration: NotRequired[CheckoutJSONBCheckoutConfigurationTypedDict]
    created_at: NotRequired[datetime]
    created_by: NotRequired[str]
    customer_id: NotRequired[str]
    environment_id: NotRequired[str]
    expires_at: NotRequired[datetime]
    r"""ExpiresAt is required. A Temporal timer fires at this time for any
    session still in initiated|pending, marking it expired. The caller
    must create a new session after expiry (expire-and-restart model).
    """
    failure_reason: NotRequired[str]
    r"""FailureReason is a human-readable string set on failed sessions."""
    failure_url: NotRequired[str]
    id: NotRequired[str]
    idempotency_key: NotRequired[str]
    r"""IdempotencyKey is caller-supplied. It is unique only while the session
    is active (initiated|pending). The same key may be reused once the
    session reaches a terminal state (completed|failed|expired).
    """
    metadata: NotRequired[Dict[str, str]]
    payment_action: NotRequired[PaymentActionTypedDict]
    payment_provider: NotRequired[CheckoutPaymentProvider]
    provider_result: NotRequired[CheckoutJSONBCheckoutProviderResultTypedDict]
    result: NotRequired[CheckoutJSONBCheckoutResultTypedDict]
    status: NotRequired[Status]
    success_url: NotRequired[str]
    r"""Redirect URLs sent to the payment provider. The provider redirects the
    user browser to the appropriate URL after the payment flow completes.
    """
    tenant_id: NotRequired[str]
    updated_at: NotRequired[datetime]
    updated_by: NotRequired[str]


class CheckoutSessionResponse(BaseModel):
    action: Optional[CheckoutAction] = None

    cancel_url: Optional[str] = None

    cancelled_at: Optional[datetime] = None

    checkout_invoice_id: Optional[str] = None
    r"""CheckoutInvoiceID and CheckoutPaymentID are set once the apply step
    creates the corresponding Flexprice entities (completed sessions only).
    """

    checkout_payment_id: Optional[str] = None

    checkout_status: Optional[CheckoutStatus] = None

    completed_at: Optional[datetime] = None

    configuration: Optional[CheckoutJSONBCheckoutConfiguration] = None

    created_at: Optional[datetime] = None

    created_by: Optional[str] = None

    customer_id: Optional[str] = None

    environment_id: Optional[str] = None

    expires_at: Optional[datetime] = None
    r"""ExpiresAt is required. A Temporal timer fires at this time for any
    session still in initiated|pending, marking it expired. The caller
    must create a new session after expiry (expire-and-restart model).
    """

    failure_reason: Optional[str] = None
    r"""FailureReason is a human-readable string set on failed sessions."""

    failure_url: Optional[str] = None

    id: Optional[str] = None

    idempotency_key: Optional[str] = None
    r"""IdempotencyKey is caller-supplied. It is unique only while the session
    is active (initiated|pending). The same key may be reused once the
    session reaches a terminal state (completed|failed|expired).
    """

    metadata: Optional[Dict[str, str]] = None

    payment_action: Optional[PaymentAction] = None

    payment_provider: Optional[CheckoutPaymentProvider] = None

    provider_result: Optional[CheckoutJSONBCheckoutProviderResult] = None

    result: Optional[CheckoutJSONBCheckoutResult] = None

    status: Optional[Status] = None

    success_url: Optional[str] = None
    r"""Redirect URLs sent to the payment provider. The provider redirects the
    user browser to the appropriate URL after the payment flow completes.
    """

    tenant_id: Optional[str] = None

    updated_at: Optional[datetime] = None

    updated_by: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "action",
                "cancel_url",
                "cancelled_at",
                "checkout_invoice_id",
                "checkout_payment_id",
                "checkout_status",
                "completed_at",
                "configuration",
                "created_at",
                "created_by",
                "customer_id",
                "environment_id",
                "expires_at",
                "failure_reason",
                "failure_url",
                "id",
                "idempotency_key",
                "metadata",
                "payment_action",
                "payment_provider",
                "provider_result",
                "result",
                "status",
                "success_url",
                "tenant_id",
                "updated_at",
                "updated_by",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
