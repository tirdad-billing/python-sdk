"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .bucket import Bucket, BucketTypedDict
from .commitmenttype import CommitmentType
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class TimeOfDayBucketTypedDict(TypedDict):
    commitment_type: NotRequired[CommitmentType]
    commitment_value: NotRequired[str]
    end: NotRequired[BucketTypedDict]
    id: NotRequired[str]
    r"""ID is server-assigned. Stable for the lifetime of the line item;
    invoice breakdown and analytics responses reference this ID.
    """
    overage_factor: NotRequired[float]
    price_id: NotRequired[str]
    r"""PriceID is the SUBSCRIPTION-scoped price created at bucket-creation time.
    Immutable post-create; changing pricing requires a successor line item.
    """
    start: NotRequired[BucketTypedDict]
    true_up_enabled: NotRequired[bool]


class TimeOfDayBucket(BaseModel):
    commitment_type: Optional[CommitmentType] = None

    commitment_value: Optional[str] = None

    end: Optional[Bucket] = None

    id: Optional[str] = None
    r"""ID is server-assigned. Stable for the lifetime of the line item;
    invoice breakdown and analytics responses reference this ID.
    """

    overage_factor: Optional[float] = None

    price_id: Optional[str] = None
    r"""PriceID is the SUBSCRIPTION-scoped price created at bucket-creation time.
    Immutable post-create; changing pricing requires a successor line item.
    """

    start: Optional[Bucket] = None

    true_up_enabled: Optional[bool] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "commitment_type",
                "commitment_value",
                "end",
                "id",
                "overage_factor",
                "price_id",
                "start",
                "true_up_enabled",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
