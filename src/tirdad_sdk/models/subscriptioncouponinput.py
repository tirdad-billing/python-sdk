"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class SubscriptionCouponInputTypedDict(TypedDict):
    coupon_code: str
    r"""CouponCode is the coupon's human-readable code (case-insensitive). Required."""
    end_date: NotRequired[datetime]
    r"""EndDate is when the coupon ends; overrides duration_in_periods calculation."""
    price_id: NotRequired[str]
    r"""PriceID is the price ID of the line item to target; omit for subscription-level."""
    start_date: NotRequired[datetime]
    r"""StartDate is when the coupon starts; defaults to subscription/phase StartDate."""


class SubscriptionCouponInput(BaseModel):
    coupon_code: str
    r"""CouponCode is the coupon's human-readable code (case-insensitive). Required."""

    end_date: Optional[datetime] = None
    r"""EndDate is when the coupon ends; overrides duration_in_periods calculation."""

    price_id: Optional[str] = None
    r"""PriceID is the price ID of the line item to target; omit for subscription-level."""

    start_date: Optional[datetime] = None
    r"""StartDate is when the coupon starts; defaults to subscription/phase StartDate."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["end_date", "price_id", "start_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
