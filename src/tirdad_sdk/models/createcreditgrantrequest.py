"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .creditgrantcadence import CreditGrantCadence
from .creditgrantexpirydurationunit import CreditGrantExpiryDurationUnit
from .creditgrantexpirytype import CreditGrantExpiryType
from .creditgrantperiod import CreditGrantPeriod
from .creditgrantscope import CreditGrantScope
from datetime import datetime
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class CreateCreditGrantRequestTypedDict(TypedDict):
    cadence: CreditGrantCadence
    credits: str
    name: str
    scope: CreditGrantScope
    addon_id: NotRequired[str]
    conversion_rate: NotRequired[str]
    r"""amount in the currency =  number of credits * conversion_rate
    ex if conversion_rate is 1, then 1 USD = 1 credit
    ex if conversion_rate is 2, then 1 USD = 0.5 credits
    ex if conversion_rate is 0.5, then 1 USD = 2 credits
    """
    end_date: NotRequired[datetime]
    expiration_duration: NotRequired[int]
    expiration_duration_unit: NotRequired[CreditGrantExpiryDurationUnit]
    expiration_type: NotRequired[CreditGrantExpiryType]
    metadata: NotRequired[Dict[str, str]]
    period: NotRequired[CreditGrantPeriod]
    period_count: NotRequired[int]
    plan_id: NotRequired[str]
    priority: NotRequired[int]
    start_date: NotRequired[datetime]
    subscription_id: NotRequired[str]
    topup_conversion_rate: NotRequired[str]
    r"""topup_conversion_rate is the conversion rate for the topup to the currency
    ex if topup_conversion_rate is 1, then 1 USD = 1 credit
    ex if topup_conversion_rate is 2, then 1 USD = 0.5 credits
    ex if topup_conversion_rate is 0.5, then 1 USD = 2 credits
    """


class CreateCreditGrantRequest(BaseModel):
    cadence: CreditGrantCadence

    credits: str

    name: str

    scope: CreditGrantScope

    addon_id: Optional[str] = None

    conversion_rate: Optional[str] = None
    r"""amount in the currency =  number of credits * conversion_rate
    ex if conversion_rate is 1, then 1 USD = 1 credit
    ex if conversion_rate is 2, then 1 USD = 0.5 credits
    ex if conversion_rate is 0.5, then 1 USD = 2 credits
    """

    end_date: Optional[datetime] = None

    expiration_duration: Optional[int] = None

    expiration_duration_unit: Optional[CreditGrantExpiryDurationUnit] = None

    expiration_type: Optional[CreditGrantExpiryType] = None

    metadata: Optional[Dict[str, str]] = None

    period: Optional[CreditGrantPeriod] = None

    period_count: Optional[int] = None

    plan_id: Optional[str] = None

    priority: Optional[int] = None

    start_date: Optional[datetime] = None

    subscription_id: Optional[str] = None

    topup_conversion_rate: Optional[str] = None
    r"""topup_conversion_rate is the conversion rate for the topup to the currency
    ex if topup_conversion_rate is 1, then 1 USD = 1 credit
    ex if topup_conversion_rate is 2, then 1 USD = 0.5 credits
    ex if topup_conversion_rate is 0.5, then 1 USD = 2 credits
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "addon_id",
                "conversion_rate",
                "end_date",
                "expiration_duration",
                "expiration_duration_unit",
                "expiration_type",
                "metadata",
                "period",
                "period_count",
                "plan_id",
                "priority",
                "start_date",
                "subscription_id",
                "topup_conversion_rate",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
