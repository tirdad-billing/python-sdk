"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .paymentaction import PaymentAction, PaymentActionTypedDict
from datetime import datetime
from pydantic import model_serializer
from tirdad_sdk.types import BaseModel, UNSET_SENTINEL
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class CheckoutJSONBCheckoutProviderResultTypedDict(TypedDict):
    expires_at: NotRequired[datetime]
    r"""ExpiresAt is the provider URL expiry. When set and earlier than the session expiry,
    executeCheckoutAction tightens the session expiry to match.
    """
    next_action: NotRequired[PaymentActionTypedDict]
    provider_metadata: NotRequired[Dict[str, str]]
    r"""ProviderMetadata holds provider-specific data not needed for business logic."""
    provider_payment_intent_id: NotRequired[str]
    r"""ProviderPaymentIntentID is the provider-side charge/intent ID.
    Stripe returns this at link creation (pi_xxx); others populate it from the webhook payload.
    """
    provider_session_id: NotRequired[str]
    r"""ProviderSessionID is stored in EntityIntegrationMapping at link creation.
    Stripe:   Checkout Session ID  (cs_xxx)
    Razorpay: Payment Link ID      (plink_xxx)
    Nomod:    Payment Link ID      (NOTE: webhook uses Charge ID; look up by PaymentLinkID field)
    Moyasar:  Payment ID
    """


class CheckoutJSONBCheckoutProviderResult(BaseModel):
    expires_at: Optional[datetime] = None
    r"""ExpiresAt is the provider URL expiry. When set and earlier than the session expiry,
    executeCheckoutAction tightens the session expiry to match.
    """

    next_action: Optional[PaymentAction] = None

    provider_metadata: Optional[Dict[str, str]] = None
    r"""ProviderMetadata holds provider-specific data not needed for business logic."""

    provider_payment_intent_id: Optional[str] = None
    r"""ProviderPaymentIntentID is the provider-side charge/intent ID.
    Stripe returns this at link creation (pi_xxx); others populate it from the webhook payload.
    """

    provider_session_id: Optional[str] = None
    r"""ProviderSessionID is stored in EntityIntegrationMapping at link creation.
    Stripe:   Checkout Session ID  (cs_xxx)
    Razorpay: Payment Link ID      (plink_xxx)
    Nomod:    Payment Link ID      (NOTE: webhook uses Charge ID; look up by PaymentLinkID field)
    Moyasar:  Payment ID
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "expires_at",
                "next_action",
                "provider_metadata",
                "provider_payment_intent_id",
                "provider_session_id",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
